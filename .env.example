BASE_URL="https://playwright.dev"
LOG_LEVEL="info"

# Used by scripts/aiFailureTriage.ts to request AI root-cause analysis of
# failed tests. In CI this is supplied via the ANTHROPIC_API_KEY repository
# secret; for local runs, set it here.
ANTHROPIC_API_KEY="your-anthropic-api-key"